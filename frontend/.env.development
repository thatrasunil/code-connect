# Development Environment Variables
# These are used when running `npm start` locally

# Backend API URL (Node.js backend with Socket.IO)
REACT_APP_BACKEND_URL=http://localhost:8000
REACT_APP_SOCKET_URL=http://localhost:3001

# Firebase configuration is in src/firebase.js
# No need to add Firebase env vars here unless you want to override them
