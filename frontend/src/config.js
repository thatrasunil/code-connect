const config = {
    BACKEND_URL: process.env.REACT_APP_BACKEND_URL || 'http://localhost:8000',
    SOCKET_URL: process.env.REACT_APP_BACKEND_URL || 'http://localhost:8000',
};

export default config;
